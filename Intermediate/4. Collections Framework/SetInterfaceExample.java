// Set Interface and its Methods

// The Set interface in Java is part of the Collections Framework and represents a collection that cannot contain duplicate elements. 
// It models the mathematical set abstraction. The two primary implementations of the Set interface are HashSet and TreeSet.



public class SetInterfaceExample {

    // Set Interface
    // The Set interface extends the Collection interface and inherits its methods. 
    // Sets do not allow duplicate elements, and the order of elements in a set is not guaranteed.

    // Key Methods:
    // add(E e): Adds the specified element to the set if it is not already present.
    // remove(Object o): Removes the specified element from the set if it is present.
    // contains(Object o): Returns true if the set contains the specified element.
    // size(): Returns the number of elements in the set.
    // isEmpty(): Returns true if the set contains no elements.
    // iterator(): Returns an iterator over the elements in the set.

}
