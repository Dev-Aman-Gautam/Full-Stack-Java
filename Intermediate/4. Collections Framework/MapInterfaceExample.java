// Map Interface, HashMap, and TreeMap Implementations

// The Map interface is part of the Collections Framework and represents a collection of key-value pairs.
// Each key maps to exactly one value. The primary implementations of the Map interface are HashMap and TreeMap.



// Map Interface
// The Map interface provides methods for storing and manipulating key-value pairs. Keys are unique, and each key maps to exactly one value.

public class MapInterfaceExample {
    
    // Key Methods:
    // put(K key, V value): Associates the specified value with the specified key in the map.
    // get(Object key): Returns the value to which the specified key is mapped, or null if the map contains no mapping for the key.
    // remove(Object key): Removes the mapping for a key if it is present.
    // containsKey(Object key): Returns true if the map contains a mapping for the specified key.
    // containsValue(Object value): Returns true if the map maps one or more keys to the specified value.
    // keySet(): Returns a Set view of the keys contained in this map.
    // values(): Returns a Collection view of the values contained in this map.
    // entrySet(): Returns a Set view of the mappings contained in this map.

}
